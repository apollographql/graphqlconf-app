subgraphs:
  conferences:
    routing_url: http://ignore
    schema:
      file: conferences.graphqls
  places:
    routing_url: http://ignore
    schema:
      file: places.graphqls
  entities:
    routing_url: ${env.EXPO_HOST}/api/graphql
    schema:
      subgraph_url: ${env.EXPO_HOST}/api/graphql
federation_version: =2.12.0-preview.8
