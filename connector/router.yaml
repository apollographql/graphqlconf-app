cors:
  allow_any_origin: true
supergraph:
  listen: 0.0.0.0:4000
connectors:
  preview_connect_v0_3: true
  subgraphs:
    places:
      $config:
        api_key: ${env.GOOGLE_MAPS_API_KEY}
override_subgraph_url:
  conferences.base: ${env.EXPO_HOST:-http://localhost:8081/}api/
  conferences.conf: ${env.EXPO_HOST:-http://localhost:8081/}api/conf/
