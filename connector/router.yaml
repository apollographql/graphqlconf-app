cors:
  allow_any_origin: true
supergraph:
  listen: 0.0.0.0:4000
connectors:
  preview_connect_v0_3: true
  subgraphs:
    places:
      $config:
        api_key: ${env.GOOGLE_MAPS_API_KEY}
override_subgraph_url:
  conferences.base: ${env.EXPO_HOST:-https://expo-production-e86b.up.railway.app/}api/
  conferences.conf: ${env.EXPO_HOST:-https://expo-production-e86b.up.railway.app/}api/conf/
